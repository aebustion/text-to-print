# Text-to-Print Mac Only Edition
# Install with: pip3 install -r requirements.txt

bleak>=0.21.0    # Bluetooth Low Energy library

# Beeper Desktop API (WhatsApp/Instagram/etc.)
requests>=2.31.0
python-dotenv>=1.0.0    # Loads BEEPER_ACCESS_TOKEN from a local .env file

# Optional: for printing images
Pillow>=9.0.0

# Optional: for HEIC image support (iPhone photos)
# If not installed, falls back to macOS 'sips' command
# pillow-heif>=0.10.0
